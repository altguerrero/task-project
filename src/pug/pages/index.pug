extends ../config/template
append config
  -pageTitle = 'Home'
  -pageDescription = 'Pagina inicial del Proyecto'
  -pageImage = ''

block main
  main.main
    header.header
      .container
        h1.header__brand Tareas
        .header__block-left
          .header__search
            svg.svg-icon.before
              use(href="assets/img/icons.svg#search")
          .heder__cerrar-sesion
            span Cerrar Sesión

    include ../includes/panel-menu
    include ../includes/tareas